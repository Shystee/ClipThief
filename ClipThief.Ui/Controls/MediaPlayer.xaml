<UserControl x:Class="ClipThief.Ui.Controls.MediaPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ClipThief.Ui.Controls"
             mc:Ignorable="d"
             x:Name="Root"
             d:DesignHeight="450" d:DesignWidth="800" MouseEnter="MediaPlayerOnMouseEnter"
             MouseLeave="MediaPlayerOnMouseLeave">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <MediaElement Grid.Row="0" Grid.RowSpan="2" x:Name="Player"
                      Source="{Binding ElementName=Root, Path=Source, NotifyOnSourceUpdated=True, NotifyOnTargetUpdated=True}"
                      LoadedBehavior="Manual" />
        <Grid x:Name="MediaControl" Grid.Row="1" Margin="5" Visibility="Hidden">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Play" x:Name="PlayButton" />
            <controls:RangeSlider x:Name="Timeline" Grid.Column="1" />
        </Grid>
    </Grid>
</UserControl>