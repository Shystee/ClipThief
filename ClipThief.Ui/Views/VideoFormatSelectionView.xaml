<UserControl x:Class="ClipThief.Ui.Views.VideoFormatSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*" />
            <ColumnDefinition Width="45*" />
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <ComboBox Grid.Column="0" ItemsSource="{Binding Path=VideoFormats, Mode=OneWay}" DisplayMemberPath="Quality" SelectedValue="{Binding Path=SelectedVideoFormat, Mode=OneWayToSource}"/>
        <ComboBox Grid.Column="1" ItemsSource="{Binding Path=AudioFormats, Mode=OneWay}" DisplayMemberPath="Hertz" SelectedValue="{Binding Path=SelectedAudioFormat, Mode=OneWayToSource}"/>
        <Button Grid.Column="2" Content="Select format" Command="{Binding Path=OpenVideoCuttingCommand, Mode=OneWay}" />
    </Grid>
</UserControl>