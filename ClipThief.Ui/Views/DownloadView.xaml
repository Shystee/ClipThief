<UserControl x:Class="ClipThief.Ui.Views.DownloadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ClipThief.Ui.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Download" />
        <controls:RangeSlider Grid.Row="1"
            LowerValue="30"
                              UpperValue="70"
                              Minimum="0"
                              Maximum="100" />
        <Button Grid.Row="2" Content="Select format" Command="{Binding Path=SelectFormatCommand, Mode=OneWay}" />
    </Grid>
</UserControl>